name: Baseline CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  baseline:
    uses: temitayocharles/shared-workflows/.github/workflows/baseline-reusable.yml@main
